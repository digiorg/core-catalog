apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Issue #285: one deterministic Pipeline Composition replaces the five
  # competing Compositions (base/database/service/gitea/messaging) that could
  # not be combined for a single AppClaim. See core-catalog/README.md.
  - pipeline.yaml
